services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=case-observer'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=root'
    ports:
      - '3306'
